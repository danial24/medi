import ContainerBase from './ContainerBase';
import React from 'react';

export default class Listing extends ContainerBase {
    render() {
        return (
            <div>
                Listing SCreen
            </div>
        );
    }
}